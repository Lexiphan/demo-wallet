<Project>
    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

    <PropertyGroup>
        <OutputPath>$(RootFolder)artifacts/bin/$(MSBuildProjectName)</OutputPath>
    </PropertyGroup>
</Project>
